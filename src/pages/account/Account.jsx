import { Panel } from '../../components/panel/Panel'; // COMPONENT

const Account = () => {
    return (
        <main>
            {/* COMPONENT */}
            <Panel />
        </main>
    );
};

export { Account };
